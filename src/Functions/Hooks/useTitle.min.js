import{useEffect,useRef}from"react";export default function useTitle({title:title}){const prevTitle=useRef(document.title);useEffect(()=>{const previousTitle=prevTitle.current;return document.title=`${title} | dspwebstudio`,()=>{document.title=previousTitle}},[title])}